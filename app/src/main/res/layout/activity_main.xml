<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Contenedor del ImageView con borde -->
    <FrameLayout
        android:id="@+id/imageFrameLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:background="@drawable/image_border"> <!-- Aplica el borde -->

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:contentDescription="Downloaded Image"
        android:scaleType="centerCrop"/>
</FrameLayout>

<Button
    android:id="@+id/downloadButton"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/imageFrameLayout"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="20dp"
    android:text="Descargar Imagen"/>

<TextView
    android:id="@+id/sensorDataTextView"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/downloadButton"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="20dp"
    android:text="Sensor Data"
    android:textSize="18sp"
    android:visibility="gone"/> <!-- Hace que no sea visible -->
</RelativeLayout>



